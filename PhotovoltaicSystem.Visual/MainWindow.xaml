<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="580"/>



        </Grid.ColumnDefinitions>
        <StackPanel Background="#2c3e50" Grid.Column="0">
            <TextBlock Text="Solar Panels.Inc" Foreground="White" FontSize="26" FontWeight="Bold" Margin="10,0,0,30"/>
            <Button Content="Sistema Fotovoltaico"
            Style="{DynamicResource SidebarButtonStyle}" Margin="0,0,0,10" FontSize="18" Click="sistema_fotovoltaico"/>
            <Button Content="Paneles" FontSize="18"
            Style="{DynamicResource SidebarButtonStyle}" Margin="0,0,0,10" Click="Panel"/>
            <Button Content="Inversores" Click="inversor"
            Style= "{DynamicResource SidebarButtonStyle}" Margin="0,0,0,10" FontSize="18" />
            <Button Content="Salir" Margin="0,0,0,10" FontSize="18"  Click="Salir"/>
        </StackPanel>
        <StackPanel x:Name="stack" Grid.Column="1" Margin="10">
            <TextBlock Text="Bienvenido a nuestra empresa" FontSize="26" 
FontWeight="SemiBold" Foreground="#34495e" Margin="80,40,100,0"/>

        </StackPanel>

    </Grid>
    <Window.Resources>
        <Style x:Key="SideBarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Left"/>
            <Setter Property="Padding" Value="10 5"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter VerticalAlignment="Center" Margin="10,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#34495e"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1abc9c"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DropShadowEffect x:Key="DropShadowEffect" Color="#888" BlurRadius="10" ShadowDepth="2"/>
    </Window.Resources>
</Window>


